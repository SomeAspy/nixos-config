{
  services = {
    printing.enable = false; # who the fuck cares (explicit disable so I can do this quickly when I inevitably have to print something every once in a blue moon)
    fwupd.enable = true; # putting in global even though firmware capsules on desktop is basically non-existent, heres to hoping
    displayManager.ly.enable = true;
  };
}
